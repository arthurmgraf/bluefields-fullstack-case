# codemap-tool is a separate public repo (JamsusMaximus/codemap). Referenced via URL.
codemap-tool/

# Claude Code template's own git history (we want the .claude *content*, not its git)
.claude/.git/

# Personal interview-prep notes (job posting, source repo URL, original v1 brainstorm).
# Kept locally for reference; excluded from the public repo so the project is reusable
# as a portfolio piece independent of this hiring case.
notes/

# Inner Next.js project's deps (don't track twice)
startup-tracker/node_modules/
startup-tracker/.next/
startup-tracker/.vercel/

# Node / Next.js (for when bluefields-tracker is created)
node_modules/
.next/
.vercel/
.turbo/
dist/
build/

# Env
.env
.env.local
.env.*.local
!.env.example

# IDE / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
